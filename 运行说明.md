# xinghe-helper 运行说明

> 口语拉练营住宿分房工具:学生扫码填表 → AI 自动分房 → 一键导出名单。
> 本文档对应 Day 7 完成的第一版 MVP(能本地跑通全流程)。

## 一、环境准备(只做一次)

1. **Node.js 18 及以上**(开发时用的是 22)
2. 一个 Supabase 项目(免费):https://supabase.com
3. 依赖安装:终端进入 `web/` 目录,执行 `npm install`

## 二、数据库(已完成,换机器时才需要重做)

建表脚本在 `supabase/migrations/`,**按文件名顺序**在 Supabase 的 SQL Editor 里依次执行:

1. `0001_schema.sql` —— 建 4 张表(camps / rooms / students / assignment_history)+ 种子营期
2. `0002_policies.sql` —— 配置行级安全(RLS)

执行完在 Supabase 的 **Project Settings → API** 页拿到两个值:

- **Project URL**
- **anon public** 密钥(设计上公开安全,写权限由数据库策略限制)

## 三、配置连接(只做一次)

在 `web/` 目录新建文件 `.env`(它被 `.gitignore` 拦截,不会进仓库),写入两行:

```
VITE_SUPABASE_URL=你的 Project URL
VITE_SUPABASE_ANON_KEY=你的 anon 密钥
```

⚠️ 改过 `.env` 后必须**重启** dev server 才生效(Vite 只在启动时读取)。

## 四、启动

```bash
cd web
npm install        # 首次或依赖变化时
npm run dev        # 启动开发服务器
```

浏览器打开 `http://localhost:5173`,三个页面:

| 路径 | 页面 | 给谁用 |
|---|---|---|
| `/form` | 学生填写页(移动优先) | 学生,手机打开 |
| `/workbench` | 分房工作台(桌面优先) | 组织者,电脑操作 |
| `/overview` | 名单总览与打印导出 | 组织者,电脑操作 |

## 五、走通全流程的验收路径

1. `/form` 用手机(或窄窗口)提交几个学生(故意混性别、有打呼/睡眠差的)
2. `/workbench` 「+ 添加房间」录入几间房 → 点「一键 AI 分房」→ 拖拽微调
3. `/overview` 切换两种视图 → 点「导出 / 打印名单」看打印预览

## 六、技术栈与目录

- 前端:React 18 + Vite 5 + react-router 6
- 数据:Supabase(Postgres,匿名读写由 RLS 策略控制)
- 分房引擎:`shared/assignEngine.js`(纯函数,规则:红线男女不混 → 睡眠相容 → 同老师同班优先 → 拼房意愿)

```
xinghe-helper/
├── AGENTS.md            # 协作规则
├── PRD.md               # 产品需求文档 v1.2
├── TECH_DESIGN.md       # 技术设计 v1.1
├── research.md          # 竞品研究
├── shared/
│   └── assignEngine.js  # 分房引擎(纯函数)
├── supabase/migrations/ # 建库 SQL(按序号执行)
└── web/                 # 前端项目
    ├── .env.example     # 环境变量模板
    └── src/
        ├── api/         # client.js / students.js / rooms.js
        └── pages/       # StudentForm / Workbench / Overview
```

## 七、上线(下一步,未做)

部署目标是 Vercel(静态托管)+ 现有 Supabase,届时把 `.env` 里的两个值配到 Vercel 的环境变量即可。上线后学生扫码填表、组织者开工作台,与开发者电脑无关。
